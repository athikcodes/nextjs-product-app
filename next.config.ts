import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["i.ibb.co", "source.unsplash.com", "images.unsplash.com", "picsum.photos" ,"i.ibb.co.com",],
  },
};

export default nextConfig;
